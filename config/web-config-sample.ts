// pillars-web-config.ts is not committed to git.
// It's created during the build process depending on the environment.

// export class PillarsWebConfig {
//     public static readonly AudioUrl = 'http://127.0.0.1:8080/';
// }